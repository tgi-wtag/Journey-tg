<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t('journal_entry.index') %></title>
</head>
<body>
  <div class="journal-entries">
    <h1><%= t('journal_entry.index') %></h1>
    
    <ul>
      <% @journal_entries.each do |entry| %>
        <li class="entry">
          <h2><%= entry.title %></h2>
          <p><%= t('journal_entry.date') %>: <%= l(entry.date) %></p>
          <p><%= t('journal_entry.content') %>: <%= entry.content %></p>
        </li>
      <% end %>
    </ul>
    
    <p><%= link_to t('journal_entry.new'), new_journal_entry_path, class: 'new-entry-button' %></p>
  </div>
</body>
</html>
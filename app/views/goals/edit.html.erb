<div class="container">
  <div class="form-container">
    <%= simple_form_for [@user, @goal], url: user_goal_path(@user, @goal) do |f| %>
      <h1><%= t('goal.edit') %></h1>
      <%= f.input :description, as: :text, input_html: { rows: 5 } %>
      <%= f.input :deadline, as: :date %>
      <%= f.select :status, Goal.statuses.keys.to_a.map { |status| [t("goal.statuses.#{status}"), status] } %><br>
      <div class='actions'>
        <%= f.submit t('goal.update_goal'), class: 'button' %>
        <%= link_to t('goal.back_to_goal_details'), user_goal_path(@user, @goal), class: 'button' %>
      </div>
    <% end %>
  </div>
</div>

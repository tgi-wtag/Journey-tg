<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t('placeholders.tasks') %></title>
  <%= stylesheet_link_tag 'entries' %>
</head>
<body>
  <div class="content-container">
    <%= link_to new_user_task_path(current_user), class: "content-card new-task" do %>
      <%= t('tasks.new_task') %>
    <% end %>

    <% @tasks.each do |task| %>
      <%= link_to user_task_path(current_user, task), class: "content-card task-link" do %>
        <h2><%= task.title %></h2>
        <p><%= task.status %></p>
      <% end %>
    <% end %>
  </div>
</body>
</html>
